	/////////////////////////////////////////////////////////////////////////////////////////////////////////////
	// RegexpValidatorInput与StringValidatorInput验证控件默认不能为空//				
	/////////////////////////////////////////////////////////////////////////////////////////////////////////////
	
	//验证表单或容器内的输入框是否正确
	//容器内的所有验证通过返回true，只要有一个没有验证通过则返回false
	
	//1.ValidatorUtils.validateAll(group) 
	//2.ValidatorUtils.validateAllByID(regexpInput,this)//单个验证，ID可为字符串可为实例
	//3.ValidatorUtils.validateAllByID("regexpInput",this)
	//4.ValidatorUtils.validateAllByID(["regexpInput","stringInput"],this)			
	
	/////////////////////////////////////////////////////////////////////////////////////////////////////////////
	
	//对于每个输入验证框可以手动验证，取得验证标识，如验证无误则返回true
	//regexpInput.validatorFlag
	
	/////////////////////////////////////////////////////////////////////////////////////////////////////////////
	
	//已可以进行手动验证
	//regexpInput.manualValidate();
	
	/////////////////////////////////////////////////////////////////////////////////////////////////////////////				
	
	//RegexpValidatorInput验证控件，高级验证。验证规则为正则表达式验证
	//errorMessage:验证失败时提示信息(验证失败)
	//regexp:正则表达式，类型可为String或RegExp
	
	/////////////////////////////////////////////////////////////////////////////////////////////////////////////
	
	//StringValidatorInput验证控件，简单的封装。最大值和最小值验证
	//maxLength:最大字符数
	//minLength:最小字符数
	//tooLongError:超过最大字符数提示信息(验证失败)
	//tooShortError:小于最小字符数提示信息(验证失败)
	
	/////////////////////////////////////////////////////////////////////////////////////////////////////////////